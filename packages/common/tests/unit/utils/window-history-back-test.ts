import windowHistoryBack from 'dummy/utils/window-history-back';
import { module, test } from 'qunit';

module('Unit | Utility | window-history-back', function (hooks) {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = windowHistoryBack();
    assert.ok(result);
  });
});
